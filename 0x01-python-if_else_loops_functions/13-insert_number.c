#include "lists.h"

/**
 * insert_node - inserts a node into a sorted array
 * @head: the linked list to modify
 * @number: to insert into the appropriate location in the list
 *
 * Return: the lionked list with the added node
 */
listint_t *insert_node(listint_t **head, int number)
{
	return (head)
}
